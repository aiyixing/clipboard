# Clipboard Monitor 使用文档

## 1. 简介

Clipboard Monitor 是一个 Windows 剪贴板监听工具，可以自动记录你复制的所有文本和图片到 Markdown 文件中。

### 功能特性

- ✅ **自动监听剪贴板** - 实时检测剪贴板变化
- ✅ **文本记录** - 复制的文本自动保存到 Markdown 文件
- ✅ **图片保存** - 截图或复制的图片自动保存到子目录
- ✅ **链接提取** - 自动提取剪贴板中的 URL 链接
- ✅ **自动去重** - 相同内容不会重复记录
- ✅ **系统托盘** - 常驻系统托盘，不打扰你的工作
- ✅ **钩子脚本** - 支持自定义脚本在剪贴板变化时执行

---

## 2. 安装与启动

### 2.1 系统要求

- **操作系统**: Windows 10 或 Windows 11
- **WebView2**: 系统已安装（Windows 11 自带，Windows 10 可能需要安装）

### 2.2 运行程序

#### 开发环境运行

```powershell
# 进入项目目录
cd clipboard

# 启动开发服务器
npm run tauri dev
```

#### 使用编译好的可执行文件

```powershell
# 运行调试版本
.\src-tauri\target\debug\clipboard-monitor.exe

# 或运行生产版本
.\src-tauri\target\release\clipboard-monitor.exe
```

### 2.3 安装程序

如果你已经生成了安装包：

- **MSI 安装包**: `src-tauri\target\release\bundle\msi\ClipboardMonitor_0.1.0_x64_en-US.msi`
- **NSIS 安装包**: `src-tauri\target\release\bundle\nsis\ClipboardMonitor_0.1.0_x64-setup.exe`

双击安装包并按照提示安装即可。

---

## 3. 界面说明

### 3.1 系统托盘

程序启动后会自动最小化到系统托盘。

**托盘图标右键菜单**:

| 菜单项 | 功能 |
|--------|------|
| 显示主窗口 | 打开主界面 |
| 暂停/恢复监听 | 暂停或恢复剪贴板监听 |
| 退出 | 完全退出程序 |

### 3.2 主界面

主界面包含以下部分：

#### 标题栏
- 应用名称: "Clipboard Monitor"
- 最小化按钮: 最小化到托盘
- 关闭按钮: 关闭到托盘（不退出）

#### 状态栏
- **运行中** (绿色圆点): 正在监听剪贴板
- **已停止** (灰色圆点): 监听已暂停

#### 当前剪贴板内容区域
- **复制测试文本 (点击: N)**: 点击此按钮可以复制测试文本到剪贴板（用于测试监听功能）
- **预览区**: 显示当前剪贴板内容（文本或图片）

#### 历史记录区域
- **打开数据目录**: 打开数据存储文件夹
- **历史列表**: 显示最近的剪贴板记录

---

## 4. 功能说明

### 4.1 文本记录

当你复制任何文本时：

1. 程序会自动检测到剪贴板变化
2. 文本内容会被记录到 `clipboard_history.md` 文件
3. 记录格式：
   ```markdown
   ## 2026-04-22 10:30:45
   这是你复制的文本内容...
   ---
   ```

### 4.2 图片保存

当你截图或复制图片时：

1. 程序会自动检测到剪贴板变化
2. 图片会被保存到 `images/` 子目录，命名为 `{timestamp}.png`
3. 图片路径会被记录到 `clipboard_history.md` 文件
4. 记录格式：
   ```markdown
   ## 2026-04-22 10:30:00
   ![图片](images/1776820350908.png)
   ---
   ```

### 4.3 链接提取

当你复制包含 URL 的文本时：

1. 程序会自动提取所有 URL 链接
2. 链接会被保存到 `links.txt` 文件
3. 相同的链接不会重复记录
4. 记录格式：
   ```text
   https://example.com/page1
   https://example.com/page2
   ```

### 4.4 自动去重

相同的内容不会被重复记录：

- 如果剪贴板内容与上次记录相同，程序会跳过
- 这可以防止多次复制相同内容时产生重复记录

### 4.5 测试功能

主界面上有一个"复制测试文本"按钮，可以用于测试监听功能：

1. 点击按钮，会复制递增的文本到剪贴板
2. 格式：`测试文本 {count} - {time}`
3. 按钮显示点击次数
4. 如果监听功能正常，你应该能在预览区看到新内容

---

## 5. 数据存储

### 5.1 数据目录位置

程序会自动选择一个可写的目录作为数据存储位置。

**选择顺序**：

1. **文档目录**: `C:\Users\{用户名}\Documents\ClipboardMonitor`
2. **用户主目录**: `C:\Users\{用户名}\ClipboardMonitor`
3. **当前工作目录**: `{程序所在目录}\data\ClipboardMonitor`（开发环境）

**如何确认实际目录**：
1. 点击主界面上的"打开数据目录"按钮
2. 或查看程序日志中的 `App data directory` 信息

### 5.2 目录结构

```
{数据目录}/
├── clipboard_history.md   # 剪贴板历史记录（Markdown 格式）
├── links.txt              # 提取的链接（去重）
├── images/                # 图片存储目录
│   ├── 1776820350908.png
│   ├── 1776820353087.png
│   └── ...
└── hooks/                 # 钩子脚本目录（可选）
    └── example.ps1
```

### 5.3 查看历史记录

你可以用任何文本编辑器打开 `clipboard_history.md` 查看历史记录。

推荐使用支持 Markdown 的编辑器：
- Visual Studio Code
- Typora
- Markdown 编辑器
- 或直接用 Chrome/Edge 浏览器打开

---

## 6. 钩子脚本 (高级功能)

### 6.1 什么是钩子脚本

钩子脚本是自定义的 PowerShell 脚本，在每次剪贴板变化时自动执行。

### 6.2 如何使用

1. 在数据目录下创建 `hooks/` 文件夹
2. 在 `hooks/` 文件夹中创建 `.ps1` 文件
3. 每次剪贴板变化时，程序会自动执行这些脚本

### 6.3 环境变量

脚本可以访问以下环境变量：

| 环境变量 | 描述 |
|----------|------|
| `CLIPBOARD_CONTENT` | 当前剪贴板内容 |
| `CLIPBOARD_TYPE` | 内容类型：`"text"` 或 `"image"` |
| `CLIPBOARD_PATH` | 图片路径（仅当类型为 image 时） |

### 6.4 示例脚本

创建文件 `hooks/notify.ps1`：

```powershell
# 示例：在剪贴板变化时显示通知
$content = $env:CLIPBOARD_CONTENT
$type = $env:CLIPBOARD_TYPE

Write-Host "剪贴板变化检测到！"
Write-Host "类型: $type"
Write-Host "内容: $content"

# 你可以在这里添加自定义逻辑
# 例如：发送到 API、保存到数据库等
```

---

## 7. 常见问题

### 7.1 程序启动后没有显示窗口

程序默认启动后最小化到系统托盘。

**解决方法**：
1. 查看系统托盘区（屏幕右下角）
2. 找到 Clipboard Monitor 图标
3. 左键或右键点击图标
4. 选择"显示主窗口"

### 7.2 剪贴板内容不更新

如果发现程序没有检测到剪贴板变化：

**检查步骤**：

1. **确认监听状态**
   - 查看状态栏是否显示"运行中"（绿色圆点）
   - 如果显示"已停止"，点击状态区域恢复监听

2. **使用测试按钮**
   - 点击主界面上的"复制测试文本"按钮
   - 观察预览区是否更新
   - 如果更新，说明监听功能正常

3. **检查日志**
   - 以调试模式运行程序：
     ```powershell
     $env:RUST_LOG = "debug"
     .\src-tauri\target\debug\clipboard-monitor.exe
     ```
   - 查看控制台输出是否有错误

### 7.3 找不到数据目录

**解决方法**：
1. 打开主界面
2. 点击"打开数据目录"按钮
3. 文件资源管理器会自动打开数据目录

或者，你可以在以下位置查找：
- `C:\Users\{用户名}\Documents\ClipboardMonitor`
- `C:\Users\{用户名}\ClipboardMonitor`
- `{程序目录}\data\ClipboardMonitor`

### 7.4 图片没有保存

**可能原因**：
1. 图片格式不支持
2. 数据目录没有写入权限

**解决方法**：
1. 确认数据目录可写
2. 使用"打开数据目录"按钮确认目录位置
3. 查看程序日志是否有错误

### 7.5 程序无法启动

**可能原因**：
1. 缺少 WebView2 运行时
2. 另一个实例正在运行

**解决方法**：

**WebView2 问题**：
- Windows 11 自带 WebView2
- Windows 10 可能需要手动安装
- 从 [Microsoft 官网](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) 下载安装

**多个实例问题**：
- 查看任务管理器
- 结束所有 `clipboard-monitor.exe` 进程
- 重新启动程序

### 7.6 性能问题

程序设计为轻量级应用，不会显著影响系统性能。

**如果发现卡顿**：
1. 检查是否有大量图片被复制
2. 定期清理 `images/` 目录中的旧图片
3. 可以暂停监听（点击状态栏）

---

## 8. 高级技巧

### 8.1 备份历史记录

定期备份数据目录是个好习惯：

1. 打开数据目录
2. 复制整个文件夹到安全位置
3. 包括：
   - `clipboard_history.md`
   - `links.txt`
   - `images/` 目录

### 8.2 清理旧数据

数据目录可能会随着时间增长，你可以：

1. 定期删除 `images/` 目录中不需要的旧图片
2. 编辑 `clipboard_history.md` 删除不需要的记录
3. 注意：删除图片后，Markdown 文件中的图片链接会失效

### 8.3 用 VS Code 查看历史

推荐使用 Visual Studio Code 查看历史记录：

1. 安装 VS Code
2. 安装 Markdown 预览插件（通常自带）
3. 打开数据目录
4. 右键 `clipboard_history.md` → "Open with Code"
5. 点击右上角的预览按钮查看格式化的 Markdown

---

## 9. 快捷键

目前没有全局快捷键。你可以：

- 通过系统托盘图标操作
- 或通过主界面按钮操作

---

## 10. 技术支持

### 10.1 收集诊断信息

报告问题时，请提供以下信息：

1. **Windows 版本**
   - 按 `Win + R`，输入 `winver`，回车

2. **程序版本**
   - 查看 `SPEC.md` 或 `package.json`

3. **程序日志**
   - 以调试模式运行程序：
     ```powershell
     $env:RUST_LOG = "debug"
     .\src-tauri\target\debug\clipboard-monitor.exe
     ```
   - 复制控制台输出

### 10.2 已知问题

1. **WebView2 冲突**
   - 在某些情况下，多个 Tauri 应用可能冲突
   - 解决：关闭其他 Tauri 应用后重试

2. **权限问题**
   - 在某些受限环境中，数据目录可能无法写入
   - 解决：程序会自动尝试多个目录，最终使用当前工作目录

---

## 11. 更新日志

### v0.1.0 (2026-04-22)

- 初始版本发布
- 基础剪贴板监听功能
- 文本和图片记录
- 链接自动提取
- 系统托盘集成
- 主界面实现
- 数据目录自动选择
- 自动测试功能

---

## 附录

### A. Markdown 格式说明

`clipboard_history.md` 使用标准 Markdown 格式：

```markdown
## 时间戳
内容（文本或图片引用）
---
```

**示例**：

```markdown
## 2026-04-22 10:30:45
这是一段复制的文本...
---

## 2026-04-22 10:29:30
![截图](images/1776820350908.png)
---
```

### B. 命令行参数

当前版本不支持命令行参数。所有配置通过程序界面操作。

### C. 文件关联

程序不会自动关联任何文件类型。你可以手动设置：

1. 右键点击 `.md` 文件
2. 选择"打开方式"
3. 选择你喜欢的 Markdown 编辑器
