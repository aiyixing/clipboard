# 剪贴板监听工具 (Clipboard Monitor)

## 1. 项目概述

**项目名称**: ClipboardMonitor
**项目类型**: 桌面工具 (Windows)
**技术栈**: Rust + Tauri 2.x
**核心功能**: 监听系统剪贴板，自动记录文本/图片到 Markdown 文件，支持插件钩子扩展
**目标用户**: 需要自动记录剪贴板历史内容的用户

## 2. UI/UX 规格

### 2.1 窗口结构

- **主窗口**: 单窗口应用，隐藏到系统托盘
- **托盘图标**: 常驻托盘，左键显示主窗口，右键显示菜单
- **窗口尺寸**: 480x640 像素，固定大小，不可调整

### 2.2 视觉设计

**色彩方案**:
- 主色: `#1a1a2e` (深蓝黑)
- 次色: `#16213e` (深蓝)
- 强调色: `#0f3460` (蓝色)
- 文字色: `#e8e8e8` (浅灰白)
- 边框色: `#2a2a4a` (灰紫)
- 成功色: `#4ade80` (绿色)
- 警告色: `#f59e0b` (橙色)

**字体**:
- 主字体: "Segoe UI", system-ui, sans-serif
- 代码字体: "Cascadia Code", "Consolas", monospace

**间距系统**:
- 基础单位: 8px
- 内边距: 16px
- 元素间距: 8px
- 区块间距: 16px

### 2.3 组件列表

| 组件 | 描述 | 状态 |
|------|------|------|
| 标题栏 | 显示应用名称和最小化/关闭按钮 | 默认、悬停 |
| 状态指示器 | 显示监听状态 (运行中/已停止) | 运行中(绿色)、已停止(灰色) |
| 剪贴板预览区 | 显示当前剪贴板内容 | 空、有内容 |
| 历史记录列表 | 显示记录的条目 | 正常、悬停、选中 |
| 清空按钮 | 清空预览区内容 | 默认、悬停、禁用 |
| 设置按钮 | 打开设置面板 | 默认、悬停 |

## 3. 功能规格

### 3.1 核心功能

#### 3.1.1 剪贴板监听
- 实时监听系统剪贴板变化
- 支持文本和图片两种类型
- 监听频率: 500ms 轮询

#### 3.1.2 数据存储
- **文本记录**: 保存到 `{data_dir}/clipboard_history.md`
- **图片存储**: 保存到 `{data_dir}/images/` 子目录
- **图片命名**: `{timestamp}.png`
- **MD格式**:
  ```markdown
  # Clipboard History

  ## 2026-04-21 10:30:45
  [图片](images/1713579045000.png)
  或者
  文本内容...
  ```
- **去重机制**: 记录前检查最近100条，相同内容不重复记录

#### 3.1.3 链接提取功能
- 自动检测剪贴板中的 URL 链接
- 提取的链接保存到 `{data_dir}/links.txt`
- 每行一个链接，自动去重

#### 3.1.4 钩子/脚本功能
- **钩子目录**: `{data_dir}/hooks/`
- **支持脚本类型**: `.ps1` (PowerShell)
- **触发时机**: 每次获取新的剪贴板内容时
- **环境变量**: `CLIPBOARD_CONTENT` - 当前剪贴板内容
- **环境变量**: `CLIPBOARD_TYPE` - "text" 或 "image"
- **环境变量**: `CLIPBOARD_PATH` - 图片路径(如果是图片)

### 3.2 系统集成

#### 3.2.1 系统托盘
- 启动时最小化到托盘
- 托盘菜单项:
  - 显示主窗口
  - 暂停/恢复监听
  - 退出

#### 3.2.2 窗口行为
- 关闭按钮: 最小化到托盘，不退出
- 最小化按钮: 最小化到托盘

### 3.3 界面交互

#### 3.3.1 主界面布局
```
┌─────────────────────────────┐
│ Clipboard Monitor    [_][X] │  <- 标题栏
├─────────────────────────────┤
│ ● 运行中                    │  <- 状态指示
├─────────────────────────────┤
│ 当前剪贴板内容:              │
│ ┌─────────────────────────┐ │
│ │                         │ │
│ │   (预览区)              │ │
│ │                         │ │
│ └─────────────────────────┘ │
│ [清空预览]                   │
├─────────────────────────────┤
│ 历史记录:                    │
│ ┌─────────────────────────┐ │
│ │ - 2026-04-21 10:30 ...  │ │
│ │ - 2026-04-21 10:29 ...  │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

#### 3.3.2 清空功能
- 点击"清空预览"后，预览区显示为空
- 清空后不再显示当前剪贴板内容，直到有新内容复制
- 历史记录列表不受影响

## 4. 数据流与模块设计

### 4.1 模块结构

```
src-tauri/
├── main.rs              # 入口点
├── lib.rs               # 库入口
├── clipboard/
│   ├── mod.rs           # 模块定义
│   ├── monitor.rs       # 剪贴板监听
│   └── storage.rs       # 数据存储
├── hooks/
│   ├── mod.rs           # 模块定义
│   └── runner.rs        # 钩子执行器
├── tray/
│   └── mod.rs           # 托盘管理
└── commands.rs          # Tauri 命令

src/
├── main.tsx             # React 入口
├── App.tsx              # 主应用
├── components/
│   ├── TitleBar.tsx     # 标题栏
│   ├── StatusBar.tsx    # 状态栏
│   ├── Preview.tsx      # 预览区
│   └── HistoryList.tsx  # 历史记录
└── styles.css           # 全局样式
```

### 4.2 关键 API

#### Rust 端命令
- `get_clipboard_content()` -> `String`
- `get_history(count: usize)` -> `Vec<HistoryItem>`
- `clear_preview()`
- `toggle_monitoring()`
- `get_monitoring_status()` -> `bool`

### 4.3 事件流
```
剪贴板变化 -> 检测内容 -> 检查重复 -> 存储数据
                                -> 提取链接 -> 保存到 links.txt
                                -> 触发钩子脚本
                                -> 通知前端更新
```

## 5. 配置与数据目录

### 5.1 数据目录结构
```
{app_data_dir}/
├── clipboard_history.md   # 文本历史
├── links.txt              # 提取的链接
├── images/                # 图片目录
│   ├── 1713579045000.png
│   └── ...
└── hooks/                 # 钩子脚本目录
    └── example.ps1
```

## 6. 验收标准

### 6.1 功能验收
- [ ] 应用启动后自动最小化到托盘
- [ ] 托盘图标显示正确，右键菜单可用
- [ ] 复制文本后，内容自动记录到 MD 文件
- [ ] 复制图片后，图片保存到 images/ 目录，路径写入 MD
- [ ] 相同内容不会重复记录
- [ ] 复制包含链接的内容时，链接自动提取到 links.txt
- [ ] hooks/ 目录下的 .ps1 脚本在新内容时被执行
- [ ] 主界面正确显示当前剪贴板内容
- [ ] 点击"清空预览"后，预览区清空且不再自动更新

### 6.2 视觉验收
- [ ] 界面颜色与设计规格一致
- [ ] 状态指示器正确显示运行状态
- [ ] 历史记录列表正常滚动
- [ ] 窗口可正常最小化和关闭(到托盘)

### 6.3 稳定性验收
- [ ] 长时间运行无内存泄漏
- [ ] 剪贴板内容较大时不卡顿
- [ ] 异常内容(如二进制)不会导致崩溃
