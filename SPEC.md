# 剪贴板监听工具 (Clipboard Monitor)

## 1. 项目概述

**项目名称**: ClipboardMonitor  
**项目类型**: 桌面工具 (Windows)  
**技术栈**: Rust + Tauri 2.x + React + TypeScript  
**核心功能**: 监听系统剪贴板，自动记录文本/图片到 Markdown 文件，支持插件钩子扩展  
**目标用户**: 需要自动记录剪贴板历史内容的用户  
**当前版本**: 0.1.0  
**状态**: 开发中 (基础功能已实现)

---

## 2. UI/UX 规格

### 2.1 窗口结构

- **主窗口**: 单窗口应用，隐藏到系统托盘
- **托盘图标**: 常驻托盘，左键显示主窗口，右键显示菜单
- **窗口尺寸**: 480x640 像素，固定大小，不可调整

### 2.2 视觉设计

**色彩方案**:
- 主色: `#1a1a2e` (深蓝黑)
- 次色: `#16213e` (深蓝)
- 强调色: `#0f3460` (蓝色)
- 文字色: `#e8e8e8` (浅灰白)
- 边框色: `#2a2a4a` (灰紫)
- 成功色: `#4ade80` (绿色)
- 警告色: `#f59e0b` (橙色)

**字体**:
- 主字体: "Segoe UI", system-ui, sans-serif
- 代码字体: "Cascadia Code", "Consolas", monospace

**间距系统**:
- 基础单位: 8px
- 内边距: 16px
- 元素间距: 8px
- 区块间距: 16px

### 2.3 组件列表

| 组件 | 描述 | 状态 |
|------|------|------|
| 标题栏 | 显示应用名称和最小化/关闭按钮 | ✅ 已实现 |
| 状态指示器 | 显示监听状态 (运行中/已停止) | ✅ 已实现 |
| 剪贴板预览区 | 显示当前剪贴板内容 | ✅ 已实现 |
| 历史记录列表 | 显示记录的条目 | ✅ 已实现 |
| 复制测试按钮 | 复制测试文本到剪贴板（带点击计数） | ✅ 已实现 |
| 打开数据目录按钮 | 打开数据存储目录 | ✅ 已实现 |

---

## 3. 功能规格

### 3.1 核心功能

#### 3.1.1 剪贴板监听

- **实现方式**: 使用 `clipboard-rs` 库的 `ClipboardWatcher` 监听剪贴板变化事件
- **监听机制**: 事件驱动（而非轮询），更高效可靠
- **支持类型**: 文本和图片
- **去重机制**: 相同内容不会重复记录（比较最近的内容）

#### 3.1.2 数据存储

- **数据目录优先级**:
  1. `{document_dir}/ClipboardMonitor` - 文档目录
  2. `{home_dir}/ClipboardMonitor` - 用户主目录
  3. `{current_dir}/data/ClipboardMonitor` - 当前工作目录（开发环境默认）

- **文本记录**: 保存到 `{data_dir}/clipboard_history.md`
- **图片存储**: 保存到 `{data_dir}/images/` 子目录
- **图片命名**: `{timestamp}.png`
- **MD格式**:
  ```markdown
  # Clipboard History

  ## 2026-04-22 10:30:45
  [图片](images/1713579045000.png)
  或者
  文本内容...
  ```

#### 3.1.3 链接提取功能

- 自动检测剪贴板中的 URL 链接
- 提取的链接保存到 `{data_dir}/links.txt`
- 每行一个链接，自动去重

#### 3.1.4 钩子/脚本功能

- **钩子目录**: `{data_dir}/hooks/`
- **支持脚本类型**: `.ps1` (PowerShell)
- **触发时机**: 每次获取新的剪贴板内容时
- **环境变量**:
  - `CLIPBOARD_CONTENT` - 当前剪贴板内容
  - `CLIPBOARD_TYPE` - "text" 或 "image"
  - `CLIPBOARD_PATH` - 图片路径(如果是图片)

### 3.2 系统集成

#### 3.2.1 系统托盘

- 启动时最小化到托盘
- 托盘菜单项:
  - 显示主窗口
  - 暂停/恢复监听
  - 退出

#### 3.2.2 窗口行为

- 关闭按钮: 最小化到托盘，不退出
- 最小化按钮: 最小化到托盘

### 3.3 界面交互

#### 3.3.1 主界面布局

```
┌─────────────────────────────┐
│ Clipboard Monitor    [_][X] │  <- 标题栏
├─────────────────────────────┤
│ ● 运行中                    │  <- 状态指示
├─────────────────────────────┤
│ 当前剪贴板内容:              │
│ [复制测试文本 (点击: 0)]     │  <- 测试按钮
│ ┌─────────────────────────┐ │
│ │                         │ │
│ │   (预览区)              │ │
│ │                         │ │
│ └─────────────────────────┘ │
├─────────────────────────────┤
│ 历史记录:                    │
│ [打开数据目录]               │
│ ┌─────────────────────────┐ │
│ │ - 2026-04-22 10:30 ...  │ │
│ │ - 2026-04-22 10:29 ...  │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

#### 3.3.2 复制测试功能

- 点击"复制测试文本"按钮，会复制递增的文本到剪贴板
- 显示点击次数，方便测试监听功能
- 格式: `测试文本 {count} - {time}`

---

## 4. 数据流与模块设计

### 4.1 模块结构

```
src-tauri/
├── main.rs              # 入口点
├── lib.rs               # 库入口，定义公共类型和状态
├── clipboard/
│   ├── mod.rs           # 模块定义
│   ├── monitor.rs       # 剪贴板监听（使用 clipboard-rs）
│   └── storage.rs       # 数据存储
├── hooks/
│   ├── mod.rs           # 模块定义
│   └── runner.rs        # 钩子执行器
├── tray/
│   └── mod.rs           # 托盘管理
└── commands.rs          # Tauri 命令接口

src/
├── main.tsx             # React 入口
├── App.tsx              # 主应用组件
├── components/
│   ├── TitleBar.tsx     # 标题栏
│   ├── StatusBar.tsx    # 状态栏
│   ├── Preview.tsx      # 预览区
│   └── HistoryList.tsx  # 历史记录列表
└── styles.css           # 全局样式
```

### 4.2 关键 API

#### Rust 端命令

| 命令 | 参数 | 返回值 | 描述 |
|------|------|--------|------|
| `get_clipboard_content` | - | `ClipboardContent` | 获取当前剪贴板内容 |
| `get_history` | `count?: number` | `HistoryItem[]` | 获取历史记录 |
| `clear_preview` | - | - | 清空预览区 |
| `toggle_monitoring` | - | `bool` | 切换监听状态 |
| `get_monitoring_status` | - | `bool` | 获取监听状态 |
| `open_data_directory` | - | - | 打开数据目录 |
| `copy_to_clipboard` | `text: string` | - | 复制文本到剪贴板 |

### 4.3 事件流

```
剪贴板变化 (Windows 事件)
    ↓
clipboard-rs ClipboardWatcher 检测
    ↓
读取剪贴板内容 (文本/图片)
    ↓
检查是否重复 (与 last_content 比较)
    ↓
├─ 是重复 → 跳过
└─ 非重复 → 继续
    ↓
├─ 存储数据 → clipboard_history.md / images/
├─ 提取链接 → links.txt
├─ 触发钩子 → hooks/*.ps1
├─ 更新状态 → current_content / history
└─ 通知前端 → emit("clipboard-changed")
```

---

## 5. 配置与数据目录

### 5.1 数据目录结构

```
{data_dir}/
├── clipboard_history.md   # 文本历史记录
├── links.txt              # 提取的链接（去重）
├── images/                # 图片目录
│   ├── 1776820350908.png
│   └── ...
└── hooks/                 # 钩子脚本目录
    └── example.ps1        # 示例脚本（可选）
```

### 5.2 数据目录选择逻辑

程序会按以下顺序尝试数据目录，直到找到一个可用的：

1. **文档目录**: `C:\Users\{username}\Documents\ClipboardMonitor`
2. **用户主目录**: `C:\Users\{username}\ClipboardMonitor`
3. **当前工作目录**: `{project_dir}\data\ClipboardMonitor`（开发环境）

**注意**: 程序会自动检测目录是否可写，如果不可写会尝试下一个目录。

---

## 6. 验收标准 (已完成)

### 6.1 功能验收

- [x] 应用启动后自动最小化到托盘
- [x] 托盘图标显示正确，右键菜单可用
- [x] 复制文本后，内容自动记录到 MD 文件
- [x] 复制图片后，图片保存到 images/ 目录，路径写入 MD
- [x] 相同内容不会重复记录
- [x] 复制包含链接的内容时，链接自动提取到 links.txt
- [ ] hooks/ 目录下的 .ps1 脚本在新内容时被执行
- [x] 主界面正确显示当前剪贴板内容
- [x] 复制测试按钮功能正常，点击次数递增

### 6.2 视觉验收

- [x] 界面颜色与设计规格一致
- [x] 状态指示器正确显示运行状态
- [x] 历史记录列表正常滚动
- [x] 窗口可正常最小化和关闭(到托盘)

### 6.3 稳定性验收

- [x] 使用 clipboard-rs 事件监听（更可靠）
- [x] 数据目录自动选择（解决权限问题）
- [x] 自动测试功能正常（5 次测试全部通过）

---

## 7. 已知问题

### 7.1 WebView2 问题

- **问题**: 开发环境中有时会出现 `请求的资源在使用中` 错误
- **原因**: 可能是另一个 Tauri 实例或 WebView2 进程仍在运行
- **影响**: 主窗口可能无法显示，但后台剪贴板监听功能正常
- **解决**: 关闭所有 Tauri 实例后重新启动

### 7.2 文件权限问题

- **问题**: 在某些系统中，`AppData/Local` 目录可能无法写入
- **解决**: 程序会自动尝试多个目录，最终使用当前工作目录下的 `data/ClipboardMonitor`

---

## 8. 变更日志

### v0.1.0 (2026-04-22)

#### 已完成

- 基础剪贴板监听功能（使用 `clipboard-rs` 事件监听）
- 文本和图片记录到 Markdown 文件
- 图片保存到 `images/` 子目录
- 自动去重功能
- 链接自动提取到 `links.txt`
- 系统托盘集成
- 主界面（预览区、历史记录、状态指示）
- 复制测试按钮（带点击计数）
- 数据目录自动选择（解决权限问题）
- 自动测试功能（验证监听可靠性）

#### 待完善

- 钩子脚本功能（需要进一步测试）
- 安装程序
- 配置文件
